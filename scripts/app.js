//here we declare our angular module. by calling angular.module()
//the first parameter is the name of our module.
//the second parameter is an array of other modules we want to inject into our app.
var app = angular.module('awesomeApp', ['ngCookies']);
